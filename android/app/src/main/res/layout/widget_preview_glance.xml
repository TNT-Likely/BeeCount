<?xml version="1.0" encoding="utf-8"?>
<!--
    桌面小组件「预览卡」——仅用于 Android 12+ 选择器 android:previewLayout。
    真实组件内容是运行时渲染的图片(见 beecount_widget.xml 的 ImageView),
    放上桌面前那张图不存在,若直接把真实布局当预览会是空白;这里用一张自包含的
    静态卡(组件名 + 一句描述)让选择器可预览、可区分。
    像素级好看的预览应改用静态截图 PNG 作 previewImage(需真机导出)。
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:padding="12dp">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/widget_name"
            android:textColor="#F5A623"
            android:textSize="18sp"
            android:textStyle="bold"
            android:gravity="center" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="@string/widget_description"
            android:textColor="#8C8674"
            android:textSize="12sp"
            android:gravity="center" />
    </LinearLayout>
</FrameLayout>
